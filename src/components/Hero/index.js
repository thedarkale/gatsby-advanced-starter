import React from "react"
import styled from "styled-components"

const Container = styled.section`
  
`

const Hero = () => {
  return (
    <Container>
      <span>Hero</span>
    </Container>
  )
}

export default Hero
